{
  "name": "mnl-dev-telemetry-vscode",
  "displayName": "MnlDevTelemetry",
  "description": "One-click MnlDevTelemetry setup and current-task visibility, right in the editor.",
  "version": "0.2.0",
  "//": "private:true keeps this out of npm; `vsce package` is unaffected by it. Set a real marketplace publisher id before `vsce publish` (docs/phase-6-notes.md).",
  "private": true,
  "publisher": "mnl-dev-telemetry",
  "icon": "icon.png",
  "license": "UNLICENSED",
  "repository": {
    "type": "git",
    "url": "https://github.com/abelbesas/mnl-dev-telemetry.git",
    "directory": "packages/vscode-extension"
  },
  "engines": {
    "vscode": "^1.85.0",
    "node": ">=20"
  },
  "categories": ["Other"],
  "keywords": ["mnl-dev-telemetry", "telemetry", "time tracking", "jira", "tempo"],
  "main": "./dist/extension.js",
  "activationEvents": ["onStartupFinished"],
  "contributes": {
    "commands": [
      {
        "command": "mnlDevTelemetry.enable",
        "title": "Enable MnlDevTelemetry on this machine",
        "category": "MnlDevTelemetry"
      },
      {
        "command": "mnlDevTelemetry.status",
        "title": "Show status",
        "category": "MnlDevTelemetry"
      },
      {
        "command": "mnlDevTelemetry.openDashboard",
        "title": "Open dashboard",
        "category": "MnlDevTelemetry"
      },
      {
        "command": "mnlDevTelemetry.openCurrentTask",
        "title": "Open current task",
        "category": "MnlDevTelemetry"
      },
      {
        "command": "mnlDevTelemetry.uninstall",
        "title": "Uninstall from this machine",
        "category": "MnlDevTelemetry"
      }
    ],
    "configuration": {
      "title": "MnlDevTelemetry",
      "properties": {
        "mnlDevTelemetry.dashboardUrl": {
          "type": "string",
          "default": "https://mnl-dev-telemetry-dashboard.vercel.app",
          "markdownDescription": "Base URL of your MnlDevTelemetry dashboard. Used for the device-auth activation page, dashboard/task links, and stored in `~/.devpulse/credentials` when you enable MnlDevTelemetry.",
          "scope": "machine"
        }
      }
    }
  },
  "scripts": {
    "build": "node esbuild.mjs",
    "watch": "node esbuild.mjs --watch",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "package": "node esbuild.mjs --production && vsce package --no-dependencies"
  },
  "dependencies": {
    "@mnl-dev-telemetry/setup": "workspace:*",
    "@mnl-dev-telemetry/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "@types/vscode": "1.85.0",
    "@vscode/vsce": "^3.2.1",
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}

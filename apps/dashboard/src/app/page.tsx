export default function Home() {
  return (
    <main style={{ fontFamily: "system-ui", padding: "2rem" }}>
      <h1>DevPulse</h1>
      <p>
        Ingestion API is live at <code>POST /api/ingest/events</code>. Dashboard
        UI arrives in Phase 4.
      </p>
    </main>
  );
}
